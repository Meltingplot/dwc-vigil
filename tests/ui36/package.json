{
  "name": "dwc-vigil-tests-ui36",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "description": "Jest suite for src/ui36 — a nested project because Vue 2.7 and Vue 3 cannot both be `vue` in one package.json, and the repo root has to be the Vue 3 one (it is what DWC 3.7's plugin builder reads).",
  "scripts": {
    "test": "jest --verbose"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "@jest/globals": "^29.7.0",
    "@vue/test-utils": "^1.3.6",
    "@vue/vue2-jest": "^29.2.6",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "vue": "^2.7.16",
    "vue-template-compiler": "^2.7.16",
    "vuetify": "^2.7.2",
    "vuex": "^3.6.2"
  }
}
